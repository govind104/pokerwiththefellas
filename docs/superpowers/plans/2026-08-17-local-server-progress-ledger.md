# Local Server (Plan 3) — SDD Progress Ledger

Plan: docs/superpowers/plans/2026-08-17-local-server.md
Branch: local-server

Task 1: complete (commits 0420651..db7ea30, review clean)
  Open Minor carried to final review: package-lock.json committed alongside Task 1 (not in the brief's Step 6 file list) -- correct/expected since `npm install` for the new workspace member updates it; not a defect, just a literal deviation from the listed files.
Task 2: complete (commits db7ea30..23e19e9, review clean)
  Open Minor carried to final review: task-2-report.md has a cosmetic line-count discrepancy (84 vs actual 68), report text only, not a code defect.
Task 3: complete (commits 23e19e9..e465088, review clean after 1 fix round)
  Two out-of-brief enabling fixes (commit afdbaa0, disclosed by implementer as DONE_WITH_CONCERNS, independently re-verified by reviewer): game-engine/src/index.ts was missing createDeck/shuffle re-exports (plan assumed they existed; they didn't), and server/tsconfig.json needed its include glob widened to reach game-engine's pokersolver.d.ts ambient declaration. Both zero-behavior-change, verified via git-stash isolation to be newly-surfaced by this task, not pre-existing.
  1 Important finding (plan-mandated, my own math error): independent-shoe test's comment claimed a collision was "astronomically unlikely" -- actually ~1.92%/run (6/312=1/52). Fixed (commit e465088) by dropping the Math.random override entirely and relying on the file's already-deterministic default (verified to produce different shoes across sequential calls). Re-review: Approved.
  Open Minor findings carried to final review: (a) unused beforeEach import (cosmetic); (b) join()'s TOCTOU race is broader than duplicate-names alone, unreachable until Task 8 wires concurrent socket calls; (c) startHand may want splitting into per-game helpers once Tasks 4-7 add more branching to the same file; (d) no non-contiguous-seating test for the button-index translation yet; (e) makeDeterministicRandom's doc comment is now stale (still describes the Math.random pattern the fix removed).
Task 4: complete (commits e465088..e1f9fa5, review clean after 1 fix round)
  1 Important finding (plan-mandated): mid-hand join() (intentionally unguarded -- joining mid-hand is correct UX) plus submitAction's Blackjack branch deriving its seat-advancement list from live this.seats instead of the fixed dealt set could crash+stall the table if a new player joined between two other players' turns. Fixed (commit e1f9fa5) by making advancePastSettledBlackjackRounds take no parameter and derive dealtSeatIndices from this.blackjackRounds.keys() internally; new regression test added. Re-review: Approved, no remaining issues, traced both pre/post-fix code paths by hand.
  Also fixed (commit b5b2119, plan-doc only): updated Task 6's not-yet-implemented call site to match the new parameterless signature, so a future dispatch doesn't reintroduce the removed parameter.
  1 justified test addition beyond the brief: split-hand payout summing (no required test exercised a split; independently verified correct by the reviewer via hand-simulation).
  Open Minor findings carried to final review: (a) duplicated between-hands reset logic (settleHoldem vs finishBlackjackHandIfComplete), verbatim brief code; (b) pre-existing await-window race in startHand (handInProgress=true set before awaits populate the hand/rounds) -- inherited from Task 3, revisit in Socket.IO/crash-recovery tasks; (c) table.ts now ~260 lines / ~10 methods, still cohesive, watch as Tasks 5-7 add more.

CHECKPOINT (user usage-limit heads-up at 80%, after Task 4 fix+re-review): 4 of 10 tasks complete and committed on branch local-server through commit b5b2119. Remaining: Tasks 5-10 (disconnect/reconnect, crash recovery, getStateForSeat, Socket.IO wiring, integration tests x2), final whole-branch review, PR creation/review/merge.
Task 5: complete (commits b5b2119..c5f7b70, review clean after 1 fix round -- opus-tier reviewer both passes)
  3 CRITICAL findings (plan-mandated, not implementer errors -- the brief's own verbatim design had these gaps): (C1) the ready-gate deadlock this task exists to close was only half-closed -- a disconnect AFTER the remaining seats were already ready never re-triggered the start-hand check; (C2) a grace-timer racing an in-flight submitAction could settle a Hold'em hand TWICE, doubling every payout -- invisible to tests since fakes resolve on microtasks, only reachable with real fs I/O; (C3) a disclosed "harmless" double-disconnect timer leak actually crashes the process via an unhandled rejection.
  2 Important findings: (I1) no error boundary on the fire-and-forget auto-act timer chain; (I2) timedOutSeats survives seat recycling, could auto-fold an innocent new occupant of a reused seat index.
  All 5 fixed in one commit (c5f7b70): startHandIfEveryoneReady() extracted and called from setReady/disconnect/leave; holdemSettled idempotence guard on settleHoldem (mirrors blackjackSettledSeats); disconnect() clears existing timer before arming a new one; .catch() added to the timer chain; leave()/both settlement paths clear timedOutSeats. Implementer did a non-vacuousness check (reverted fix, confirmed all 4 new tests fail for the predicted reason, restored). C2's test used a ControllableHandLog fake proven (by the re-reviewer, independently traced) to genuinely suspend one submitAction mid-flight while a second settlement runs to completion -- real overlap, not two sequential calls.
  Re-review (opus): Approved, no remaining Critical/Important. 3 Minor findings carried to final review: (a) one test's comment describes a technically-wrong mechanism (the assertion itself is still valid/stronger than claimed); (b) disconnect()/leave() can return with handInProgress=true and holdemHand=null for one tick (only observable if a caller reads state synchronously in the same tick -- Task 8 awareness); (c) pre-existing gap, more reachable now: a rejected hand-start (e.g. HoldemHand's constructor throwing on a player with stack<=0, a normal outcome) permanently bricks the table since handInProgress is never reset on failure -- reviewer explicitly flagged as "worth a separate ticket," not blocking this task.
Task 6: implementation + fix done, RE-REVIEW NOT YET DISPATCHED (commits c5f7b70..1ea30ba)
  Implementer reported DONE_WITH_CONCERNS on the first pass (commit 2d417d6): correctly self-identified a real gap in the plan's own design (not their error) -- Blackjack settles per-seat, but the discard-on-replay check (`allSettled`) only looked at whether EVERY round was settled, so a crash after one seat's natural-blackjack payout was written but before another seat finished would replay to a log indistinguishable from "never paid," causing recovery to re-pay that seat. HandLog alone couldn't tell the two cases apart (a natural's settlement isn't triggered by a logged action).
  Fix (commit 1ea30ba, controller-designed, dispatched to the same implementer): settleBlackjackSeatIfNeeded now logs a `blackjack_seat_settled` marker after each real payout; recoverFromLog's Blackjack branch seeds the existing `blackjackSettledSeats` idempotence guard (already proven correct in Tasks 4-5) from those markers before calling advancePastSettledBlackjackRounds(), and the old coarse `allSettled` discard block was deleted -- superseded by per-seat idempotence. Side benefit: recovery now preserves seats (letting players reconnect normally) even when every seat was already settled, instead of wiping the table. Hold'em's recovery discard-if-settled was deliberately left as-is (safe, just more conservative -- single settlement loop, not per-seat, narrower/lower risk, not worth the scope right now).
  Tests updated/added: existing natural-blackjack-during-recovery test verified still valid unmodified; existing "discards already-fully-settled" test rewritten to assert the new preserve-seats behavior with markers present; new test added proving the exact unsafe-under-old-code scenario (no marker = never paid = safe to pay now) is now handled correctly. 58/58 tests pass x3 runs, typecheck clean.
  holdemSettled reasoning check (asked of implementer per dispatch instructions): verified clean, no gap -- recovery runs once on a fresh Table instance before anything could leave it stale.

CHECKPOINT (user usage-limit heads-up at 92%, stopping before Task 6's re-review dispatch): 6 of 10 tasks' CODE is complete and committed on branch local-server through commit 1ea30ba (Tasks 1-5 fully reviewed+approved; Task 6 implemented+fixed but its post-fix re-review has NOT been dispatched yet). Working tree is clean, nothing uncommitted.
NEXT STEP ON RESUME: generate the review package via `scripts/review-package c5f7b70 HEAD` (base = end of Task 5, so the reviewer sees Task 6's full diff including the fix in one pass) and dispatch an opus-tier task reviewer for Task 6 (elevated tier, matching Task 5's precedent, since this is also money-correctness-critical recovery/replay logic) using the task-reviewer-prompt.md template, covering both the original recoverFromLog implementation and the blackjack_seat_settled marker fix. If clean, mark Task 6 complete and proceed to Tasks 7-10 (getStateForSeat, Socket.IO wiring, 2x integration test suites), then the final whole-branch review, then the PR.

RESUMED (post-compact): review package regenerated at review-c5f7b70..1ea30ba.diff (identical range, same 2 commits). Opus-tier reviewer dispatched for Task 6 covering both the original recoverFromLog implementation and the blackjack_seat_settled marker fix, with explicit attention drawn to replay idempotence, blackjackSettledSeats-before-advance ordering, and continued safety of the unchanged Hold'em recovery branch.
Opus review result: Needs fixes. 1 Critical + 3 Important (6 Minor carried forward, not fixed this round):
  CRITICAL: the round-1 fix put the blackjack_seat_settled marker AFTER playerStore.setBalance instead of before (table.ts:331-332) -- narrows the double-payment crash window instead of closing it, exactly the failure class the marker exists to prevent. The implementer's own round-1 report had specified the correct (before) order; the fix landed the other way.
  IMPORTANT: recoverFromLog has no error handling -- a torn JSONL tail (the natural artifact of the crash it recovers from) throws uncaught on every future boot, permanently bricking the server since nothing clears the corrupt log.
  IMPORTANT: no test covers a *marked* settled seat alongside a still-in-progress seat -- the exact scenario the marker mechanism exists for was untested.
  IMPORTANT: FakeHandLog/ControllableHandLog.readAll() return live references instead of round-tripping through JSON like production's JsonlHandLog does, masking potential writer/reader shape drift.
  Two ⚠️ cannot-verify-from-diff items -- one resolved by me directly (BlackjackRound's constructor copies its shoe option via spread, packages/game-engine/src/blackjackRound.ts:40-42, so no shared-reference risk exists); one carried forward as a hard requirement for Task 8 (recoverFromLog must be called exactly once at startup before accepting connections).
Fix round 2 dispatched (sonnet, fresh implementer -- original Task 6 implementer's agent ID was lost across the compaction boundary, so this round included full context in the dispatch instead of continuing the same agent): (1) swap the two lines at table.ts:331-332 to write-ahead order; (2) wrap recoverFromLog's body in try/catch, log+clear on failure, RED/GREEN test with a malformed first entry; (3) add the marked-seat+in-progress-seat regression test; (4) fix both fake HandLogs' readAll() to JSON round-trip. All 4 fixes fully specified by me (exact before/after code for the two code changes); Minor items explicitly marked out of scope for this round. AWAITING RESULT.
NEXT STEP ON RESUME (if interrupted again before result lands): check for a completion notification first before re-dispatching -- if the agent is still running, do not duplicate it.
Fix round 2: DONE (commits 1ea30ba..a1b679a: 785eff9 order swap, 335a80e try/catch+RED/GREEN test, a7faedc marked+in-progress regression test, a1b679a fake-readAll JSON round-trip). Server 60/60, monorepo 175/175, typecheck clean both workspaces, server suite re-run 3x. I verified the diff directly (table.ts:319-333 marker-before-balance confirmed; table.ts:360-443 try/catch wraps the full body correctly) before dispatching re-review, rather than trusting the report alone.
One self-flagged nuance from the implementer, not fixed (instructed to flag only): a synthetic rounds:[] (empty-but-present) corrupted-log shape could in theory leave this.blackjackRounds/blackjackSettledSeats mutated before rounds[0] throws inside the new try block; implementer argues both reassignments are value-equivalent to fresh defaults so it's a non-issue, and the shape isn't reachable from any real write path. Asked the round-3 reviewer to independently verify this claim rather than accept it at face value (specifically: whether blackjackSettledSeats could end up non-empty via a stray blackjack_seat_settled entry in `rest` even when rounds is empty, and whether that could survive into a future hand). Carry forward to final review regardless of the reviewer's verdict on it, given how contrived the trigger is -- do not spin up a fourth round for this alone unless the reviewer elevates it to Critical/Important.
Third opus-tier review dispatched, scoped tightly to the fix-round-2 diff (1ea30ba..a1b679a) rather than the full range, since rounds 1's findings are settled context, not open questions.
Review 3 result: Needs fixes (no Critical). All 4 round-2 items confirmed correctly done and in-scope; the Critical order-swap independently re-verified correct by the reviewer tracing both interleavings themselves. 2 Important + 3 Minor:
  IMPORTANT: my own instruction to "flag rather than fix" the rounds:[] nuance led the implementer to a false dismissal -- alreadySettledSeats is NOT guaranteed empty in that case (built from `rest` independent of `rounds`), and more importantly the SAME missing-reset root cause is reachable via realistic I/O failures (playerStore.getBalance mid-loop; a throw inside settleBlackjackSeatIfNeeded during recovery's own advancePastSettledBlackjackRounds call) with no exotic log shape needed at all.
  IMPORTANT: the Critical write-order fix (round 2's whole purpose) has zero test coverage -- reverting the swap leaves all 60 tests green.
  Minor: catch's own handLog.clear() call is unguarded (defeats try/catch's purpose if the log layer itself is broken); new marked-seat test doesn't assert marker non-duplication; a new (self-correcting, low-impact) divergence if append rejects but the process survives -- deferred, same class as Task 5's I1.
Fix round 3 dispatched via SendMessage to the SAME implementer agent (ab0167e596bdc8486, still addressable in-context, no compaction since its spawn) rather than a fresh one: (1) catch block now resets seats/handInProgress/holdemHand/blackjackRounds/blackjackSettledSeats/activeSeatIndex to fresh-constructor defaults before a now-guarded handLog.clear() attempt; (2) new vi.spyOn-based test pinning append-before-setBalance call order, with an explicit revert-and-confirm-RED instruction since this is the one check that specifically re-proves the Critical fix; (3) one-line marker-count assertion bundled into the existing marked-seat test. Deferred/out of scope: the append-rejects-but-process-survives divergence (self-correcting, carried to final review). AWAITING RESULT.
NEXT STEP ON RESUME (if interrupted again): check for a completion notification before re-dispatching -- don't duplicate the agent. If this round comes back clean (Approved or only-Minor-left), mark Task 6 fully complete (final commit range c5f7b70..<round-3 HEAD>) and proceed to Task 7 (getStateForSeat) via `scripts/task-brief docs/superpowers/plans/2026-08-17-local-server.md 7`. If another round is needed, keep using SendMessage to the same agent -- do not lose track of its ID again if compaction happens; write it explicitly into the next checkpoint if a quota/context warning arrives mid-round.
Fix round 3: DONE (commits 5f49e1f, e31b14a, 36d50b7). I read both resulting diffs directly against my exact spec rather than trusting the report -- catch block matches verbatim, both test additions are correct and well-reasoned (the order-pinning test correctly attaches vi.spyOn AFTER the setup log write, so it can't be fooled by seed data). Independently re-ran the suite and typecheck myself: 61/61 server tests passing, typecheck clean. Given these fixes were narrow/mechanical (a defensive reset in an error path, two purely-additive tests) and didn't touch the already-independently-re-verified core settlement/ordering logic, judged a 4th opus review round disproportionate to the remaining risk and closed the task on my own verification instead.

Task 6: COMPLETE. Full commit range c5f7b70..36d50b7 (10 commits: 2d417d6 impl, 1ea30ba fix1, 785eff9/335a80e/a7faedc/a1b679a fix2, 5f49e1f/e31b14a/36d50b7 fix3). 3 review rounds (opus each), severity converged Critical->Important->clean. Server suite 61/61, monorepo 176/176, typecheck clean both workspaces.
Carried forward to final whole-branch review (none blocking, all explicitly deferred with reasoning across the 3 review rounds):
  - recoverFromLog doesn't cross-check the log's entry type against this.config.gameMode -- a restart with a reconfigured game mode would misreplay.
  - Hold'em recovery reassigns players to seats 0..n-1 instead of preserving original seat indices (Blackjack logs/preserves seatIndex explicitly); relatedly, Hold'em's settled-hand discard wipes the table while Blackjack's now preserves seats for the same real-world scenario -- worth reconciling.
  - 'blackjack_seat_settled' is a bare string literal duplicated across writer/reader/tests with no shared const or HandLogEntry['type'] union -- a typo would silently disable the settlement guard with no compiler signal.
  - recoverFromLog is a long method (79+ lines) with two inline game-specific branches; extracting per-game helpers + a shared seat-construction helper was suggested twice across reviews.
  - An unrecognized first log entry type falls through both branches silently without clearing the log (pairs with the now-fixed corrupted-log handling, but this specific sub-case -- well-formed JSON, unrecognized `type` string -- doesn't throw, so the new catch/reset doesn't cover it).
  - A live-play (not recovery) divergence: if handLog.append rejects but the process survives, in-memory state (blackjackSettledSeats, seat.balance) can briefly run ahead of what's persisted -- self-correcting on the seat's next setBalance, same deferred class as Task 5's I1 (no error boundary on live-play paths generally).
  - disconnectTimers/timedOutSeats are deliberately NOT reset in recoverFromLog's catch block (traced: only mutated by the trailing disconnect() loop, which runs after both branches fully succeed, and disconnect()'s only throw path is already guarded) -- flagged once more in fix round 3's own report as worth a second look if Task 8's real onStateChange wiring turns out capable of throwing, currently unverifiable since no production caller exists yet.
HARD REQUIREMENT for Task 8 (not a defect, a design constraint from the Task 6 reviewer): recoverFromLog must be called exactly once at server startup, before accepting any connections -- it is public, unguarded, and would clobber live state if called twice or mid-hand.

NEXT STEP: proceed to Task 7 (getStateForSeat) via `scripts/task-brief docs/superpowers/plans/2026-08-17-local-server.md 7`.

Task 7: brief generated (task-7-brief.md). Pre-dispatch spot-check confirmed no Task-3-style export gap: PlayerHand/RoundPhase/RoundResult/HoldemStreet/HoldemResult/Pot all exported from game-engine's index.ts, BlackjackRound.getDealerUpcard()/getDealerCards() both real methods. Brief is fully-specified verbatim code (pure/synchronous projection method, no async/timing/crash concerns) -- sonnet-tier implementer dispatched (matches the moderate-risk assessment for Tasks 7-8), with explicit context that the Blackjack-playerHands-not-viewer-filtered / Hold'em-holeCards-viewer-filtered asymmetry in the spec is intentional (real Blackjack rules: cards dealt face-up, only dealer hole card hidden) so it isn't second-guessed as a gap during self-review. Asked for an explicit security-lens self-review trace (who can see each TableStateView field, including the null/spectator viewer and folded-player-cards-stay-hidden-post-settlement cases), since this method is literally the access-control boundary Task 8 will broadcast through. AWAITING RESULT.
NEXT STEP ON RESUME (if interrupted): check for completion notification before re-dispatching.
Task 7: implementer returned NEEDS_CONTEXT (good escalation, not a mistake). Found a genuine plan-level lifecycle gap: settleHoldem/finishBlackjackHandIfComplete (built in Task 4, already reviewed) null/clear the hand data in the SAME synchronous step that settles it, so no external caller (including this task's own brief-specified test, and production's future Task 8 broadcast) can ever observe the "just settled, show results" state -- onStateChange fires once, AFTER clearing already happened. Fail-closed, not fail-open: implementer's full field-by-field security trace found zero hole-card/dealer-card over-sharing in any reachable state, confirmed independently by me reading the report. Same structural gap latently affects Blackjack's all-seats-settled case too (not caught by this brief's specific test, which only settles one of two seats).
Chose resolution (of 3 the implementer proposed without picking one themselves, correctly deferring the call): a last-settled snapshot (private lastSettledHoldemHand/lastSettledBlackjackRounds, captured right before the existing null/clear lines, read as a fallback source in getStateForSeat, cleared at the top of startHand). Chosen over "reorder onStateChange calls" (only helps a caller synchronized to the exact narrow window, doesn't fix this task's own test or support polling/reconnect/integration-test queries, needs Task-8-specific choreography) and over "accept the gap, weaken the test" (silently drops a real product behavior the engine's own docs anticipate). Deliberately scoped as in-memory-only, does NOT survive recoverFromLog (log is already fully cleared by this point in both settlement paths, unchanged by this fix; adding durability would need a new log entry type, real scope beyond this lifecycle fix; losing the reveal after a crash is a minor UX gap not a correctness one).
Verified this fix touches only ADDITIVE lines in settleHoldem/finishBlackjackHandIfComplete -- doesn't alter the 4 existing tests' pinned "holdemHand/blackjackRounds become null/empty immediately" contract, doesn't touch Task 6's hardened payment-durability ordering at all. Dispatched via SendMessage to the same implementer (af6e32c92a4bf548a, still in-context). Asked for: the existing failing test to pass; add a missing assertion (folded player's cards stay hidden post-settlement -- implementer's own flagged gap); one new Blackjack test for the all-seats-settled analog; explicit confirmation the 4 pinned-contract tests are unaffected; full monorepo suite + typecheck. AWAITING RESULT.
NEXT STEP ON RESUME (if interrupted): check for completion notification before re-dispatching -- don't duplicate the agent. After this lands, dispatch a task review (sonnet-tier -- this fix is synchronous snapshotting, no new async/timing ordering, lower risk than Task 6's class of bug, matching the moderate-risk assessment for Task 7) covering Task 7's full diff via `scripts/task-brief` + `scripts/review-package` against whatever the pre-Task-7 HEAD commit turns out to be (check `git log` -- Task 6 ended at 36d50b7).
Task 7: DONE, commit c6231c7 (single commit, table.ts + table.test.ts only). I verified the diff directly against my exact spec before dispatching review -- matches precisely (2 new private fields, capture lines in settleHoldem/finishBlackjackHandIfComplete, clear lines in startHand, fallback-source reads in getStateForSeat with the filtering body itself byte-identical to the brief). Test additions also verified directly: folded-stays-hidden assertion added to the existing settlement test; new Blackjack all-seats-settled test explicitly asserts the live map IS empty before checking the snapshot-backed view still returns data (real non-vacuousness proof). Implementer self-caught and corrected a flawed non-vacuousness check on that new test mid-verification (Observation 10 in the report) -- a good sign, but told the reviewer to check the correction critically rather than assume it's right. Server 67/67 x3 runs, monorepo 182/182, typecheck clean, the 4 tests pinning holdemHand/blackjackRounds null-immediately re-confirmed by name.
Sonnet-tier reviewer dispatched for the full diff (36d50b7..c6231c7), specifically asked to independently verify (not trust) that the snapshot can't leak stale data into a new hand, that live-vs-snapshot fallback ordering is correct in every state, and to check one named aliasing risk (lastSettledBlackjackRounds captures the Map by reference, not a copy -- asked the reviewer to check nothing mutates round objects in the old map after the swap). Also asked for an independent full trace of the hole-card visibility matrix (viewer x street x folded), not just accepting the implementer's trace. AWAITING RESULT.
Task 7 review (round 1): FAILED before producing findings -- the sonnet-tier reviewer (agent a2da80b8b215861b1) hit the session/quota limit after only a preliminary session-start check. Not a code defect; an infrastructure/quota interruption, no findings exist to act on.
Task 7 review (round 2): Approved, no Critical/Important. Reviewer (agent a8bb264da6bc86f38) independently re-verified all 3 named risks clean rather than accepting the report: no stale-data leakage across startHand's clear-order (traced the full method incl. the transient pre-deal await window, confirmed fail-closed), live-vs-snapshot fallback ordering correct in every reachable state, and the blackjackRounds aliasing risk closed (every mutating call site reads through the reassigned live field, never the archived snapshot). Also independently re-traced the full Hold'em hole-card visibility matrix (viewer x street x folded) from the diff itself, including the folded-player-always-sees-own-cards nuance and the seat-recycling case.
2 Minor findings carried to final whole-branch review: (a) recoverFromLog's catch-block reset (error-recovery path) doesn't clear the two new snapshot fields the way startHand's parallel reset does -- only reachable if recoverFromLog ever runs on a non-fresh Table instance, which Task 6's hard requirement (called exactly once at startup, before any hand is played) already forbids; same class of finding as Task 6's already-deferred disconnectTimers/timedOutSeats-not-reset gap -- revisit once Task 8's actual recoverFromLog call site exists to confirm the invariant genuinely holds; (b) file growth (table.ts 563 lines / table.test.ts 1199 lines), informational only, inherent to the brief's own file placement, no action needed.

Task 7: COMPLETE. Commit range 36d50b7..c6231c7 (1 commit). Server 67/67 x3 runs, monorepo 182/182, typecheck clean both workspaces.

NEXT STEP: proceed to Task 8 (Socket.IO wiring) via `scripts/task-brief docs/superpowers/plans/2026-08-17-local-server.md 8` -- remember the hard requirement (recoverFromLog must be called exactly once at startup, before accepting connections).

User guidance (AFK, checkpoint discipline + model-tier steer, 2026-08-18): user is going AFK and asked for more stringent/frequent checkpointing to protect against losing progress to another quota cutoff -- write ledger entries immediately on dispatch (agent id + status), not just on result. User also gave an explicit tie-breaking rule for model selection: if a task's review loop is trending toward multiple rounds, prefer fewer passes on a stronger model (sonnet/opus) over more passes on a cheaper one (haiku/sonnet) -- total pass count matters more than per-call cost. Applying this from Task 8 onward: judge each task's implementer/reviewer tier with a bias toward the stronger option when the task touches timing/connection-lifecycle logic (the class of bug that has needed multiple rounds before, e.g. Tasks 5-6).

Task 8: brief generated (task-8-brief.md, 350 lines -- protocol.ts/socketServer.ts/index.ts/socketServer.test.ts, all verbatim reference code). Pre-dispatch spot-check against current table.ts: every Table method the brief's reference code calls (join, reconnect, setReady, submitAction, leave, disconnect, recoverFromLog, getStateForSeat) matches the real signature exactly, TableConfig/TableDeps shapes match exactly, PlayerAction/HoldemAction both confirmed exported from game-engine's index.ts -- no Task-3-style export gap.
Found one real cross-task structural gap (same shape as Task 7's: an earlier already-reviewed task's code has a latent issue Task 8 is the first thing to make reachable) -- NOT an implementer error, a controller-directed fix designed before dispatch: Table.join() (table.ts:109-121, Task 3) has a TOCTOU race across its `await this.deps.playerStore.getBalance(displayName)` call. Task 3's own reviewer had flagged this as a carried-forward Minor ("join()'s TOCTOU race is broader than duplicate-names alone, unreachable until Task 8 wires concurrent socket calls") but Task 8 is exactly that trigger, and tracing it found the race is worse than that note implied: it's not just two same-displayName joins racing (both pass the pre-await duplicate check, both later write -- second throws only if checked again), it's that `seatIndex` is *also* computed before the await, so two *different* displayNames racing for the table's one open seat both compute the same seatIndex and the second silently overwrites the first's seat assignment post-await -- a silent player-displacement / data-corruption bug, not just a missed duplicate-name error. Reclassifying this from Minor to a must-fix-in-Task-8 item given the severity once traced fully.
Designed fix (to hand the implementer verbatim): move `const seatIndex = this.seats.findIndex((s) => s === null);` from before the `await` to after it, and re-run the duplicate-displayName check again immediately after the await too -- so the entire read-check-write sequence (both checks + the seat assignment) runs after the only await point, fully synchronously, making it atomic under JS's single-threaded microtask semantics regardless of how many join() calls are in flight. Also asked for two new regression tests in table.test.ts proving genuine overlap (not just sequential calls that look concurrent) via a controllable/suspendable PlayerStore fake, same pattern as Task 5-6's ControllableHandLog: (a) two concurrent same-displayName joins -- exactly one succeeds; (b) two concurrent different-displayName joins racing for one open seat -- both succeed, each in a distinct seat index.
Model tier: sonnet implementer (brief is complete reference code plus one fully-specified controller-directed fix, matches Tasks 3/4/6/7's precedent for this shape of task). Reviewer elevated to OPUS (up from the moderate-risk sonnet default used for Tasks 3/4/6/7) -- this task wires real concurrent socket connections into shared mutable Table state for the first time, is the same bug class that needed opus to catch Task 5's 3 Criticals, and per the user's fresh AFK guidance above, a task with this risk profile should get the stronger reviewer on round 1 rather than risk a multi-round sonnet loop.
Task commit scope will deviate from the brief's Step 6 file list (protocol.ts/socketServer.ts/index.ts/socketServer.test.ts only) -- table.ts and table.test.ts will also be staged and committed for the join() fix + its regression tests, disclosed the same way as Task 7's out-of-brief file.
Implementer dispatched (sonnet, agent aab5f2adbf16775a4), base commit c6231c7 (Task 7's HEAD, unchanged).
Implementer FAILED mid-work: infrastructure API error ("the response stopped arriving"), not a code defect -- same interruption class as Task 7's first review attempt, this time hitting an implementer instead. Its last progress note: "GREEN -- all 59 tests pass (57 original + 2 new race tests). Now implementing socketServer.ts and index.ts (Step 4, verbatim from brief)."
Verified real state directly via `git status`/`git diff --stat` rather than trusting the truncated report: table.ts modified (+18/-1, the join() fix), table.test.ts modified (+120 lines, the two new race regression tests) -- matches the 59/59 GREEN claim exactly (57+2). protocol.ts and socketServer.test.ts exist as new untracked files (brief Steps 1-2 done). socketServer.ts and index.ts do NOT exist yet. Nothing committed. Confirms the agent died cleanly partway through brief Step 4, before any commit (commit is Step 6) -- no corruption, no partial files, safe to resume.
Resumed the SAME agent (aab5f2adbf16775a4) via SendMessage rather than a fresh dispatch -- its context/reasoning about the join() fix and the rest of the brief is still intact, a fresh agent would need to rediscover both the fix rationale and the already-correct uncommitted work. Gave it the exact file-by-file state above and told it to continue from brief Step 4 onward (implement socketServer.ts/index.ts, run full suite, typecheck, commit with the expanded 6-file list, self-review incl. the recoverFromLog-ordering trace and socket-event security-lens check, report). AWAITING RESULT.
Resumed run completed cleanly, no further interruption. Status: DONE. Commit 551fcd2 (protocol.ts/socketServer.ts/index.ts/socketServer.test.ts new, table.ts/table.test.ts modified for the join() fix). Server 73/73 (59 table.test.ts incl. 2 new race tests, 4 socketServer.test.ts, 6 handLog.test.ts, 4 playerStore.test.ts), game-engine 115/115, typecheck clean workspace-wide.
I verified the full diff directly (review-c6231c7..551fcd2.diff) rather than trusting the report: join() fix (table.ts:504-533) matches my spec exactly -- seatIndex computation and the duplicate-name re-check both moved to after the only await, seat assignment immediately follows with no further await between recheck and write. Both new table.test.ts regression tests (lines 400-478) are well-constructed: a new ControllablePlayerStore fake suspends getBalance on command (same pattern as Task 5-6's ControllableHandLog), both tests assert pendingCount===2 before releasing (proving genuine overlap, not sequential calls), and both check actual final table.seats state, not just promise outcomes -- test 2 specifically asserts distinct seat indices and that table.seats contains both players, exactly the more-severe silently-overwrites-the-other-player bug this closes. socketServer.ts/protocol.ts/index.ts/socketServer.test.ts all byte-match the brief's reference code, no unauthorized deviation. recoverFromLog-before-any-connection ordering re-confirmed directly from the diff: httpServer.listen() appears nowhere inside createServer, only in the test and in index.ts's .then() callback, both of which only run after createServer()'s promise (which awaits recoverFromLog first) resolves.
Implementer self-review surfaced one real finding beyond the join() race, correctly did NOT fix it (out of authorized scope, correctly flagged instead): a socket that emits 'join' twice (e.g. two different displayNames on one connection) orphans its first seat -- the second join's seatBySocketId.set() overwrites the socket's seat mapping, so the first seat's `connected` flag is never flipped false by a later real disconnect (nothing maps that socket back to it anymore), permanently stranding that seat as connected-forever/un-reconnectable/uncounted toward capacity. Implementer characterized this as a low-severity "self-inflicted resource leak"; I assess it as more severe -- a permanent table-capacity loss affecting the whole table, not just the misbehaving client, though it does need a buggy or malicious client to trigger (no normal UI flow sends a second 'join' once seated). A correct fix needs more care than a simple guard (a naive `if (seatBySocketId.has(socket.id)) reject` doesn't close the case where two 'join' events race within the same socket before the first's await resolves), so it's not a trivial bolt-on like the Table.join() fix was. Deferring to final whole-branch review rather than blocking Task 8 on it: confined to code this task owns outright (not a cross-task already-merged concern), doesn't cross the money-correctness/cross-player-data-leakage bar that's triggered an immediate fix every other time this session, and Tasks 9-10 (not yet built) may end up informing the right fix shape once their own scenarios are known. Carried to final review as a named item, not a footnote.
Also noted (informational, not an issue): one harmless stderr line in socketServer.test.ts from a 50ms grace-window timer outliving that test's own cleanup -- implementer traced it as pre-existing Table behavior, not introduced by this task.
Review package generated (review-c6231c7..551fcd2.diff). Dispatched OPUS-tier reviewer (agent a3d3b251e795367d1).
Review result: Needs fixes. Full verbatim review saved to task-8-review-1.md (durable record, file:line-heavy, not worth re-pasting here). Summary: 1 Critical + 1 Important, both must-fix; 5 Minor carried forward.
CRITICAL (confirms and significantly escalates the implementer's self-flagged concern): socketServer.ts's join handler writes `seatBySocketId.set(socket.id, seatIndex)` only AFTER `table.join()`'s await resolves. If the socket disconnects during that real fs-backed await (ordinary usage -- closing the tab moments after clicking Join, not just a buggy/malicious client), the 'disconnect' handler finds no mapping yet and never calls `table.disconnect()`; the seat then lands as permanently `connected:true` with nothing able to reach it. Reviewer traced the full consequence chain past what either I or the implementer had: `startHandIfEveryoneReady` requires EVERY connected seat to be ready, and this orphaned seat can never be marked ready by anyone -- so the ENTIRE TABLE deadlocks permanently (no hand can ever start again), not just "one seat's capacity lost." Only a server restart clears it. Same root cause as the implementer-flagged same-socket-double-join variant (a mapping written post-await, nothing reconciles a seat with no live socket) -- reviewer confirmed one unified fix closes both.
IMPORTANT: `JsonlHandLog.append()` (handLog.ts) is a bare unserialized `appendFile` -- two concurrent `submitAction` calls both complete their (correctly-ordered, synchronous) `hand.act()` before either awaits its own `append()`, so the in-memory state is fine, but the two appends can land on disk in either order. Out-of-order replay throws inside `hand.act()` during `recoverFromLog`, silently discarding all recovered state via the existing catch-block reset. Reachable via a client acting before its state broadcast arrives, or via the grace-window auto-act timer firing during another player's in-flight append -- no client misbehavior required for the second trigger. Pre-existing gap in Tasks 4-6's code, but Task 8 is what first makes concurrent submitAction entry possible.
5 Minor carried to final review (not fixed this round): disconnect handler's missing try/catch (currently unreachable); stale seatBySocketId entries on the old disconnect-during-join path (no correctness impact -- note: the fix below incidentally closes this specific instance of it, since the new code never registers a mapping for a socket found already-disconnected); displayName-as-sole-reconnect-credential (accepted Task 5/6 design, surfaced once for whole-branch review); table.ts file size (579 lines, inherited); concurrency test coverage stops at 2 concurrent callers (breadth, not a proof gap).

Designed both fixes myself (both touch already-merged tasks' files, same pattern as the join() race and as Task 7's cross-task gap):

FIX 1 (Critical, socketServer.ts join handler) -- unify both orphan variants via one mechanism: after `table.join()`/`table.reconnect()` resolves to a seatIndex, (a) if this socket already had a DIFFERENT seatIndex mapped (a stale mapping from an earlier still-in-flight join on the same socket), call `table.disconnect()` on that old seat before proceeding; (b) if the socket itself is no longer connected (`!socket.connected`), call `table.disconnect(seatIndex)` on the NEW seat too and return without registering a mapping. Both branches reuse the existing, already-hardened disconnect()/grace-window/timedOutSeats machinery from Task 5 instead of inventing new cleanup logic -- an orphan-prone seat becomes an ordinary disconnected seat that either reconnects normally or times out normally.

FIX 2 (Important, handLog.ts) -- serialize `append()` via a promise chain (`private writeQueue: Promise<void>`), reassigning `this.writeQueue` SYNCHRONOUSLY (no `async`/`await` in `append()` itself, since an await here would reopen the identical race the fix closes) so a second call issued before the first's write completes still sees the updated chain and queues correctly behind it. `.catch(() => {})` on the internally-tracked queue pointer (not on the promise returned to the caller) so one failed write doesn't permanently wedge every subsequent append.

Dispatching both fixes to the SAME implementer (sonnet, agent aab5f2adbf16775a4, still addressable) via SendMessage with exact code for both, plus test requirements (concrete guidance given, some latitude on exact test mechanism left to the implementer, consistent with how well the join()-fix tests turned out). AWAITING RESULT.
Fix round: DONE, commit c8dcaa8 (socketServer.ts/handLog.ts modified, socketServer.test.ts/handLog.test.ts modified with new tests; table.ts/table.test.ts untouched as instructed). Server 76/76 (59+6+7+4), game-engine 115/115, typecheck clean; implementer confirmed non-flaky across 5 consecutive runs. HandLog fix's RED evidence: 3 consecutive genuine failures against real Windows fs I/O (different out-of-order pairs each run) before the fix landed -- the race was empirically real on this machine, not just theoretical.
I verified the diff directly (review-551fcd2..c8dcaa8.diff, scoped to just this fix round per Task 6 round-3's precedent -- round 1's already-approved parts of the diff aren't re-reviewed): both fixes match my spec exactly, byte-for-byte including comments. handLog.test.ts's new test took the real-fs repeated-trials approach (25 trials/50 concurrent appends, not fs mocking) and checks every trial's a-before-b ordering individually. socketServer.test.ts's two new tests are genuinely rigorous, not minimal: both use an independent test-owned signal (a raw `server.io.on('connection', ...)` disconnect listener, or `pendingCount` polling via `vi.waitFor`) to prove the race window is really hit before asserting anything, and both go beyond checking "seat isn't orphaned" to proving the table ISN'T DEADLOCKED afterward -- new players can still join and a hand can still start, which is the actual severe consequence the reviewer traced, not just the narrower symptom.
Implementer self-flagged one further nested edge case beyond my own trace: `table.disconnect(previousSeatIndex)` could itself throw (e.g. "Seat is empty") if that seat were independently cleared by some other path in the narrow window between being recorded in seatBySocketId and this cleanup running -- characterized it as "safely contained by the existing try/catch." I partially disagree with that framing the same way I did with "self-inflicted" in round 1: the try/catch does prevent a crash, but doesn't prevent the NEW seat (already created by the time this throws, since Table.join() unconditionally inserts it before socketServer.ts's cleanup logic runs) from ending up unregistered in seatBySocketId -- a milder, narrower recurrence of the same orphan class, requiring a very specific interleaving (same-socket double-join AND independent clearing of the first seat in between). Not spawning a 3rd fix round for this -- narrow, try/catch-contained (no crash), and chasing nested edge cases indefinitely is exactly the over-rotation the user asked me to avoid. Carrying forward to final review as a named item; also handing it to the re-reviewer for an independent severity call (same pattern as round 1's orphan-seat handoff, which is what surfaced the real Critical in the first place -- worth repeating).
Generated round-2 review package (review-551fcd2..c8dcaa8.diff). Resuming the SAME opus reviewer (a3d3b251e795367d1) via SendMessage rather than a fresh dispatch -- it already has full context on both original findings and doesn't need to re-verify the parts of the diff it already approved. AWAITING RESULT.
Round-2 review: Approved, no Critical/Important. Reviewer independently re-verified (not accepted) both fixes close what it found: traced the disconnect-during-join path to `connected:false` + grace timer armed, confirmed `startHandIfEveryoneReady`'s `connected` filter (table.ts:171) is what makes this un-deadlockable now, confirmed the phantom-ready sub-variant is also closed by the same filter (disconnected seats never reach `connectedSeats`), confirmed the fix generalizes to 3+ chained joins on one socket, and specifically confirmed a redundant same-name rejoin can't boot a player off their own seat (cleanup runs only after table.join()'s duplicate-check has already had its chance to throw -- verified the ordering is load-bearing, not incidental). handLog fix: independently re-derived the synchronicity property line-by-line (no await between reading and reassigning writeQueue) rather than trusting my spec was followed, confirmed the `.catch` is genuinely necessary (without it one failed write wedges the queue forever), confirmed JSON.stringify happening inside the .then() preserves the existing rejection-based error contract Table.submitAction relies on.
Independent verdict on the previousSeatIndex-disconnect-throws edge case: agreed my consequence-tracing was right and the implementer's "safely contained" framing was incomplete (a throw there really would skip seatBySocketId.set and leave the NEW seat exactly as orphaned as round 1's Critical) -- but traced reachability to *nil* today: the only seat-nulling site (leave()) deletes its own mapping in the same synchronous block with no interleaving window, so no live path can produce a stale previousSeatIndex pointing at a null seat right now. Correctly deferred, not a live bug. Named the underlying invariant precisely: "nothing may null a seat outside leave()'s synchronous mapping-delete block" -- and flagged that this is the SAME latent shape as round 1's Minor 3 (disconnect handler's own missing try/catch around a throwing table.disconnect) -- both go live simultaneously the moment Task 9 or 10 adds seat-reaping on grace-window expiry, which is a very plausible thing for disconnect-timeout tests to need. IMPORTANT flag for myself when scoping Tasks 9-10: watch for this specific invariant if either task's brief involves reaping/clearing a seat outside leave()'s own code path.
4 more Minor carried forward: clear() doesn't participate in the new writeQueue (latent asymmetry, nil live race today since settlement's clear() calls are only ever reached after their own append already resolved -- worth recording as an invariant given Tasks 9-10 build crash-recovery tests on this file); ControllablePlayerStore duplicated verbatim between table.test.ts and socketServer.test.ts (DRY nit, a 3rd copy would be the real pain point); handLog.test.ts's new test checks per-pair ordering, would catch drops but not duplicates (strictly optional to strengthen); more grace-timers now armed means marginally more chances for the already-known-harmless teardown stderr noise.

Task 8: COMPLETE. Full commit range c6231c7..c8dcaa8 (2 commits: 551fcd2 base implementation + join() race fix, c8dcaa8 seat-orphan fix + handLog write-ordering fix). 2 review rounds (opus both), severity converged Critical+Important -> clean. Server 76/76, monorepo 191/191, typecheck clean both workspaces.
Carried forward to final whole-branch review (9 Minor total across both rounds, plus the named invariant above): disconnect handler's own missing try/catch (round 1 Minor 3, shares the seat-nulling invariant with the edge case above); displayName as sole reconnect credential (accepted Task 5/6 design); table.ts size (579 lines); join() concurrency tests stop at 2 callers; clear() outside the writeQueue; ControllablePlayerStore duplication; handLog ordering test doesn't catch duplicates; teardown timer noise; the previousSeatIndex-disconnect-throws edge case (nil reachability today, named invariant to watch in Tasks 9-10).

NEXT STEP: proceed to Task 9 (integration tests, happy path) via `scripts/task-brief docs/superpowers/plans/2026-08-17-local-server.md 9`. Watch specifically for whether its brief adds any seat-reaping/clearing logic outside leave()'s own synchronous block -- if so, the two deferred Minors above (round-1 #3 and the previousSeatIndex edge case) may need to be reconsidered rather than left for final review.

Task 9: brief generated (task-9-brief.md, 225 lines). Adds ONE new file only (integration.test.ts, 4 fully-specified tests) -- no source changes, so the seat-reaping watch-item above does not apply to this task (confirmed, defer that check to Task 10). Pre-dispatch spot-check: 'all-in' and 'stand' both confirmed as real HoldemAction/PlayerAction literals (holdemBetting.ts:1, blackjackRound.ts:8); the brief's core mechanism claim ("once both are all-in, HoldemHand cascades straight through to settlement in one call") is independently confirmed by an existing, already-reviewed game-engine test (holdemHand.test.ts:545-572, "HoldemHand.act -- all-in runout"), not just asserted by the brief. Both error-message regex assertions (/full/, /already seated/) verified to match Table.join()'s actual exact error strings. Blackjack payout set ([975,1000,1025,1037.5] for a 25-chip bet) matches standard win/push/loss/natural-blackjack outcomes. No Task-3-style gap found.
Model tier: HAIKU implementer (complete verbatim test file, zero source changes, zero implementation judgment calls -- purely transcription + run + verify + commit, and my own pre-flight diligence already closed the main risk vectors a cheap-tier model could stumble on, per the frugality memory's own guidance to reduce brief ambiguity first rather than default to a pricier tier). SONNET reviewer (lower risk than Task 8 -- no new production/source code to scrutinize, just verifying 4 tests are well-constructed and prove what they claim; no reason to pay for opus here).
Implementer dispatched (haiku, agent abae3ed09dd0bbbaa), base commit c8dcaa8 (Task 8's HEAD). Failed almost immediately on a session/quota limit (same infrastructure-failure class as Task 7 round-1's reviewer and Task 8's mid-round implementer interruption) -- got only as far as "I'll start by reading the task brief," essentially no work done yet. Not a code/task problem. Resuming the SAME agent via SendMessage per established precedent (Task 8's implementer resumed cleanly from a similar interruption) rather than fresh-dispatching. AWAITING RESULT.
NEXT STEP ON RESUME (if interrupted again before the result lands): check for a completion notification before re-dispatching -- don't duplicate the agent (abae3ed09dd0bbbaa); resume the same agent again if it's another infrastructure failure. Resumed run completed cleanly. Status: DONE, commit 4598050. All 80 tests pass (76 prior + 4 new) in 187ms, typecheck clean. I verified the diff directly (review-c8dcaa8..4598050.diff): byte-for-byte exact match of the brief's Step 1 content, zero deviations -- exactly what a fully-specified haiku-tier transcription task should produce, and confirms the pre-flight diligence (action-literal/regex/payout verification) paid off.
Review package generated (review-c8dcaa8..4598050.diff). Dispatched SONNET-tier reviewer (agent a71c82f9bce230f34). AWAITING RESULT.
Review: Approved, no Critical/Important. Reviewer independently re-verified every load-bearing claim against actual source rather than trusting the brief's own framing or the report: traced Hold'em settlement through holdemHand.ts/holdemPots.ts to confirm [0,1000,2000] is the complete (not just plausible) outcome set and the zero-sum property is structural; traced Blackjack's payout.ts to confirm [975,1000,1025,1037.5] are the only 4 reachable payout values; traced the all-in-cascades-to-settlement mechanism through dealStreet's recursion; confirmed both rejection regexes against the actual thrown error strings; and specifically verified the deterministic seed protects BOTH seats from a natural-blackjack turn-skip (not just alice's, which is all the test file's own comment documents) by cross-referencing an existing already-passing unit test using the identical seed/scenario. 1 Minor carried forward: the seed-safety comment in integration.test.ts only explains the alice-side risk, not the symmetric bob-side risk it also happens to protect against -- documentation completeness only, not a correctness issue.
Task 9: COMPLETE. Commit range c8dcaa8..4598050 (1 commit). Full workspace 80/80 tests (187ms), typecheck clean, review clean on first pass (no fix round needed).

NEXT STEP: proceed to Task 10 (integration tests, resilience) -- the FINAL task of this plan -- via `scripts/task-brief docs/superpowers/plans/2026-08-17-local-server.md 10`. Watch closely for the seat-reaping-outside-leave() invariant flagged in Task 8's carried-forward Minors (round-1 Minor 3 + the previousSeatIndex-disconnect-throws edge case): disconnect-timeout/reconnect scenarios are exactly where a seat could get nulled/cleared outside leave()'s own synchronous mapping-delete block, which would make both of those currently-nil-reachability Minors go live. If Task 10's own brief adds such logic, reconsider those two Minors rather than leaving them for final review.

Task 10: brief generated (task-10-brief.md, 245 lines). One new file only (integration-resilience.test.ts, 4 tests: reconnect-within-grace-window, disconnect-past-grace-window, restart-persists-balances, crash-recovery-resumes-hand) -- no source changes, so this task cannot itself violate anything.
Pre-dispatch verification of the seat-reaping watch-item: grepped table.ts for every seat-nulling site -- confirmed `this.seats[seatIndex] = null` appears EXACTLY ONCE in the whole file, inside leave() (table.ts:152). onGraceWindowElapsed (table.ts:218-224, what test 2 exercises) only adds to timedOutSeats, never nulls a seat -- a timed-out seat stays occupied (auto-acted-for, not vacated), matching test 1's "reconnect within grace window resumes the SAME seat" expectation. Confirmed: Task 10 does not exercise or threaten the leave()-only-nulls-seats invariant -- both Task 8 carried-forward Minors tied to that invariant stay correctly deferred to final review, nothing to reconsider here.
Read recoverFromLog's actual Hold'em branch (table.ts:435-466) to verify test 4 (crash recovery)'s core mechanism before dispatch rather than trusting memory of Task 6's ledger notes: it reconstructs a fresh HoldemHand from the logged `holdem_hand_started` entry, then replays every `holdem_action` entry via `hand.act(playerId, action, amount)` in order -- exactly the mechanism test 4 needs for alice's single logged 'call' to leave `actingPlayerId === 'bob'` after recovery. Recovered seats are rebuilt with `connected: false` (table.ts:457-463), confirming bob's post-recovery reconnect correctly needs to go through table.reconnect() rather than a fresh join(), matching the brief's test code. Also confirmed the carried-forward Task 6 note ("Hold'em recovery reassigns players to seats 0..n-1") is real (seats rebuilt by array position, table.ts:455-464) but doesn't threaten this brief's assertions -- both test 3 and test 4 assert by displayName/playerId lookup, never by a hardcoded seat index, so seat reindexing during recovery can't break them.
Model tier: SONNET implementer (not haiku, despite the brief being fully-specified reference code same as Task 9 -- the risk here isn't implementation judgment, it's that test 4 exercises the single most heavily-hardened, highest-stakes subsystem in this whole plan end-to-end for the first time, and if it doesn't pass first try, correctly distinguishing "test mistake" from "a real last-mile gap between Table's recovery logic and the Socket.IO layer that no unit test could catch" needs more judgment than Task 9's scenarios did). OPUS reviewer (same tier as Tasks 5/6/8 -- this is exactly the turn-order/timer/disconnect/recovery risk class that has earned opus every time, and it's the final task before the whole-branch review, so getting it right in one pass matters more than usual, matching the user's fewer-stronger-passes guidance).
Implementer dispatched (sonnet, agent aacb9bd2271347770), base commit 4598050 (Task 9's HEAD). AWAITING RESULT.
Resumed cleanly, no interruption. Status: DONE, commit 76d6dee. Server 84/84 (incl. the 4 new tests, verified clean in isolation too), full monorepo 199/199 (115 game-engine + 84 server), typecheck clean both workspaces.
Notably, the implementer went beyond the committed assertions given test 4's stakes: wrote a throwaway (uncommitted, deleted before final commit) diagnostic test proving the crash-recovery scenario is genuine, not incidental -- confirmed the raw hand-log file at the crash point has exactly 2 unsettled entries (hand_started + alice's call) with no settlement marker; confirmed the post-restart Table object fails strict identity against the pre-crash instance (rules out shared in-memory state silently making the test pass); ran a negative control (blanking the log before a third restart correctly yields handInProgress:false, proving recovery isn't a no-op that always returns true). These diagnostics aren't in the final diff (correctly not committed, since they're not part of the brief) -- noted for the reviewer that these claims need independent tracing through the actual code, not just trusting the report's narrative about deleted test code the diff can't show.
I verified the diff directly (review-4598050..76d6dee.diff): byte-for-byte exact match of the brief's Step 1 content, single new file, zero deviations, no diagnostic-test leftovers.
Non-blocking concern flagged by the implementer: test 4's simulated-crash disconnects leave the pre-restart Table's 300ms grace-window timers uncancelled, theoretically could fire a stray already-caught ENOENT write after the temp dir is removed -- not observed across 3 runs, same class as already-known-harmless noise from Tasks 8-9, no production code touched.
Review package generated (review-4598050..76d6dee.diff). Review: Approved, no Critical/Important. Reviewer independently re-derived every load-bearing claim about test 4 from table.ts/socketServer.ts/holdemHand.ts source directly (not from the implementer's deleted diagnostic) and ran the new file in isolation (4/4 pass, 182ms, zero stderr) as extra corroboration given the stakes. Confirmed: actingPlayerId is never assigned anywhere in table.ts (grep-verified), so 'bob' can only come from genuine HoldemHand construction + real hand.act() replay; the log is provably unsettled at the crash point (settleHoldem/finishBlackjackHandIfComplete are the only handLog.clear() call sites, both gated on full settlement, neither reached after just alice's call since bob's BB option keeps the street at 'preflop'); recovery is a genuinely fresh object graph (Table holds zero static/module state, createServer always `new Table()`s); bobReconnect's join is confirmed to hit table.reconnect() not table.join() via the `??` (not `||`) operator at socketServer.ts:39 -- explicitly noted this matters because `||` would have broken test 1 for seat index 0. Also independently confirmed test 2's auto-fold is the semantically correct action (alice as SB facing a 5-chip toCall, checking would be illegal) and test 3's balance assertion is non-vacuous (995, not the 1000 default, so a lost-file fallback would actually fail it).
6 Minor + 2 final-review-radar items carried forward:
  Minor: abandoned pre-crash Table's 300ms grace timer still targets the SAME hand-log path after stopServer() (nothing cancels it) -- reviewer characterizes this as a more interesting risk than the implementer's "benign ENOENT" framing: a stray SUCCESSFUL append is possible in principle, not just an error, though empirically far outside the window here (29ms actual crash-to-recovery vs 300ms timer) and not a flake source in this test as constructed.
  Minor: the actingPlayerId==='bob' assertion is a weaker discriminator than it looks -- bob is also first-to-act on a contaminated post-flop state, so a stray write landing before recovery reads the log would still pass this specific assertion. Reviewer suggests street==='preflop' as a stronger future check but explicitly flags this as "not actionable under a verbatim-spec task" -- not fixed, brief is verbatim reference code.
  Minor: afterEach doesn't await server close before rm(dir) -- confirmed as the actual mechanism behind the suite's known-benign stderr class, verified non-noisy in the reviewer's own run.
  Minor: stopServer()'s second close stage (httpServer.close()) is redundant -- io.close() already closes the attached httpServer in socket.io v4; safe dead weight, not a bug.
  Minor: afterEach would throw on `server` being undefined if startServer ever failed first -- unreachable today, would mask the real failure if it ever became reachable.
  Minor: test 2's title says "hand continues" but an auto-fold heads-up necessarily ENDS the hand -- brief's own wording, not an implementer deviation.
  Coverage note (not a defect): none of Task 10's tests exercise the per-seat targeted emit or hole-card filtering from Task 7 -- every waitForState in this file resolves on the broadcast (viewer=null) state, which is valid for what's being asserted (all assertions are viewer-independent) but means Task 7's filtering has no integration-level coverage anywhere in the suite.
  FINAL-REVIEW RADAR ITEM (informational, flagged prominently by the reviewer as worth real attention, not just a footnote): Table has NO teardown/dispose API -- clearTimeout only exists inside leave/disconnect/reconnect, so nothing external can cancel a Table's pending disconnectTimers. The stray-write hazard above is this task's visible symptom, but the reviewer notes the identical shape would affect a REAL production graceful-shutdown path (index.ts currently has no SIGTERM/SIGINT handling at all) -- an abandoned Table could append to a hand log its successor is actively reading. Worth a deliberate decision at the final whole-branch review: add a stop()/dispose() method, or explicitly accept as out of scope for a local/personal-use server.
  FINAL-REVIEW RADAR ITEM: independently re-confirmed Task 6's already-carried-forward note from a different angle -- Hold'em recovery rebuilds seats by ARRAY POSITION (this.seats[i], keyed on the players array index) while Blackjack recovery preserves the original r.seatIndex. The two branches disagree on seat-index fidelity in the general case (works fine for these tests' contiguous seats 0/1; would diverge for e.g. seats 0 and 3). Two independent reviewers now flagging the same gap strengthens the case for resolving it at final review rather than deferring further.

Task 10: COMPLETE. Commit range 4598050..76d6dee (1 commit). Server 84/84, monorepo 199/199 (115 game-engine + 84 server), typecheck clean both workspaces. Review clean on first pass (no fix round needed) -- matches Task 9's precedent that a well-pre-flighted, fully-specified test-only task converges in one round even at elevated tiers.

ALL 10 TASKS OF PLAN 3 ARE NOW COMPLETE.
Merge-base confirmed: 0420651d8d204da44b931462eb0a04a338af1e44 (Task 1's start point). Full-branch review package generated: review-0420651..76d6dee.diff (24 commits, ~150KB). Compiled the complete carried-forward findings list from every task's ledger entries into its own durable file, carried-forward-findings.md (31 items, grouped into: A. architectural/production-readiness -- no Table teardown API, Hold'em/Blackjack recovery seat-index fidelity mismatch; B. a cluster of 3 items sharing one currently-holding-but-fragile invariant (nothing nulls a seat outside leave()'s synchronous block); C. other durability-adjacent deferred items; D. code structure/maintainability; E. test coverage gaps; F. one accepted-design item surfaced for awareness; G. pure cosmetics) rather than leaving 10 tasks' worth of scattered notes for the final reviewer to hunt through the ledger for.
Dispatched the final whole-branch reviewer (opus, agent a313b0f9bc03cef6c) using superpowers:requesting-code-review's code-reviewer.md template (read fresh, not from memory), pointed at both the design spec and the plan file, the review package, and the carried-forward-findings.md file with explicit instructions to form an independent severity verdict on every item rather than rubber-stamping the task-level categorization, with special attention to section A and the section-B invariant (asked to re-verify it holds across the WHOLE branch, not just within the task that originally checked it).
Failed almost immediately on an infrastructure error (same class as every other mid-session interruption -- Task 7 round-1 review, Task 8's implementer, Task 9's implementer), essentially no work done yet ("I'll start by loading the task-observer skill..."). Resuming the SAME agent via SendMessage per this session's established, repeatedly-successful precedent rather than fresh-dispatching. AWAITING RESULT.
Failed once mid-start on an infrastructure error (essentially no work done, same class as every other interruption this session), resumed via SendMessage, completed cleanly. Full verbatim review saved to final-branch-review.md (durable record).
Verdict: Ready to merge? NO -- WITH FIXES. This is the most valuable single review of the whole session: the reviewer actually stood up the real Table/PlayerStore classes and drove empirical boundary-value probes (0, negative, empty string, __proto__/constructor as displayName) rather than only reading, and found 3 Criticals that NONE of the 19 task-level review rounds caught -- because each one only becomes reachable once multiple tasks compose (e.g. C1 needs Task 4's settlement AND Task 9's specific all-in test scenario to both exist before balance=0 is reachable at all).
CRITICAL C1: a 0-chip player permanently bricks the Hold'em table -- HoldemHand's constructor throws on stack<=0, nothing resets handInProgress on that path, every exit (submitAction/leave) then throws forever, restart is the only fix. This is a NORMAL game outcome (integration.test.ts:117 itself asserts alice can end an all-in at exactly 0) -- this is carried item 11, correctly deferred at Task 5 (settlement didn't exist yet), now the single most important finding on the branch.
CRITICAL C2: Blackjack has NO affordability check at all -- balances go unbounded negative (empirically reproduced: two 0-balance players -> both end at -25 after a hand). Worse than C1 in one way: Hold'em fails loudly (brick), Blackjack silently manufactures money that doesn't exist.
CRITICAL C3: unvalidated displayName hits the prototype chain -- JsonPlayerStore.readAll() returns a plain JSON.parse'd object, so `data["constructor"]` returns the inherited FUNCTION (not undefined), `??` never fires, and Table then throws "must have a finite stack" on that function -- same permanent brick as C1, reachable from ANY unauthenticated socket given `cors: {origin:'*'}` (any web page the operator's browser visits, not just a deliberate client). Violates the design spec's own explicit Section 6 requirement that malformed payloads never reach the engine.
IMPORTANT, escalated from carried-forward items with new empirical evidence: I1 reconnect() is the one seat-state method that doesn't re-run the ready gate (deadlocks between-hands reconnect); I2 handLog.clear() bypasses the write queue -- measured 4/200 trials where a racing next-hand append gets silently wiped (item 9's "no live race" reasoning was incomplete, didn't consider the FOLLOWING hand's append, only preceding ones); I3 an unrecognized first log-entry type never clears the log, permanently poisoning recovery once composed with I2's race; I4 recoverFromLog doesn't cross-check entry type against config.gameMode, and unlike C1 this brick SURVIVES RESTARTS (a sanctioned cross-process mode-switch workflow can permanently kill the server); I5 (new finding) JsonPlayerStore has no atomic write and only catches ENOENT -- a crash mid-write leaves every future getBalance rejecting forever, the durable money file has strictly weaker protection than the transient hand log; I6 no error boundary around settleHoldem -- a rejected setBalance leaves holdemSettled=true but handInProgress stuck true forever, same brick class as C1 via the money path; I7 a torn JSONL trailing line (the canonical crash artifact) discards the WHOLE hand not just the last action, contradicting the spec's stated "loses at most one in-flight action" guarantee; I8 zero integration-level coverage exists anywhere for hole-card filtering -- every integration test happens to only assert viewer-independent fields.
MOST VALUABLE SINGLE FINDING: re-verified the shared "nothing nulls a seat outside leave()'s block" invariant (which 2 separate opus Task-8 reviews certified as holding, and which 3 Task-8/7 Minors were deferred against) across the WHOLE branch rather than just within Task 8 -- and found it is ALREADY FALSE at HEAD. recoverFromLog's catch block (table.ts:508, added during Task 6's LATER fix rounds, after the Task 8 reviews certified the invariant) does a wholesale `this.seats = new Array(...).fill(null)`. The 3 deferred Minors stay non-live for a DIFFERENT, weaker, completely undocumented reason instead (createServer's recoverFromLog-before-connection-registration ordering) -- a future refactor reordering that would silently activate all 3 at once with no test failing. This is exactly the cross-task-composition blind spot task-level review structurally cannot see, caught only because I asked this final review to re-verify the invariant across the whole branch rather than trust the earlier certification.
Full carried-forward-findings.md triage (31 items, all independently re-verdicted): 4 items escalated from Minor/deferred to must-fix given branch-level reachability (3->I4, 8->I6, 9->I2, 10->I3, 11->C1, 19->I8 -- six escalations total); everything else confirmed as legitimate follow-up, with several sharpened (item 2's seat-index-fidelity note didn't mention buttonSeatIndex isn't restored at all; item 7's list of un-reset fields on the recovery catch path was incomplete -- also missing holdemSettled/timedOutSeats/disconnectTimers/buttonSeatIndex, not just the 2 originally named).
Reviewer's own top process recommendation, worth remembering for future plans: "probe the composed system" (empirically drive real classes with boundary values, not just read) should be a standing step of final whole-branch reviews specifically, since per-task review cannot see cross-task composition by construction -- 19 careful task-level rounds read this exact code and none found C1.

Designed all fixes myself against the current, freshly-read table.ts/handLog.ts/playerStore.ts (not just the reviewer's line numbers) before dispatching, grouped by shared root cause per the reviewer's own Recommendation #1/#2 and the skill's one-fixer-not-one-per-finding guidance:
  Group 1 (table.ts): new eligibleSeatsForHand() filter (balance>0 holdem / >=blackjackDefaultBet blackjack) wired into startHandIfEveryoneReady, closing C1+C2 at the root; reconnect() now calls startHandIfEveryoneReady (closes I1); startHand's risky section wrapped in try/catch with full state reset + log clear on any construction failure (C1 defense-in-depth); settleHoldem's payout loop now catches each player's setBalance independently inside a try/finally so one rejection can't block others or leave handInProgress stuck (closes I6); settleBlackjackSeatIfNeeded given the identical treatment on its own setBalance call (same bug class I found in the parallel Blackjack settlement path during design, not explicitly named by the reviewer -- disclosed as an addition, write-ahead marker-before-balance ordering explicitly preserved).
  Group 2 (handLog.ts + table.ts + playerStore.ts): clear() rewritten to route through the same synchronous writeQueue chain as append() (closes I2); readAll() now tolerates one unparseable TRAILING line only, still throws on interior corruption (closes I7); recoverFromLog's two branch conditions gained a gameMode cross-check plus a new else branch that warns+clears on any unrecognized-or-mismatched first entry (closes I3+I4 together); JsonPlayerStore rewritten for atomic writes (temp file + rename) and corruption tolerance, plus null-prototype balance maps so no displayName can ever resolve to an inherited Object.prototype member (closes I5 and the storage-layer half of C3).
  Group 3 (socketServer.ts): new isValidDisplayName() guard (non-empty string, <=32 chars) rejecting bad payloads in the join handler before they reach Table at all (closes the network-boundary half of C3, defense-in-depth alongside Group 2's storage-layer fix).
  Group 4 (integration.test.ts): new hole-card-filtering assertions on an existing Hold'em hand-start scenario, checked from both players' own received state (closes I8).
  Group 5 (socketServer.ts): comment on createServer documenting the recoverFromLog-before-connection-registration invariant the final review found undocumented (Recommendation #3).
Full exact code specified for every change (matching this session's established practice of designing precise fixes myself, not forwarding raw findings) plus specific test requirements per group, with some implementation latitude explicitly granted where the exact test mechanism is a judgment call.
Dispatched ONE opus-tier fix subagent (agent a69a75ef8c61f8baa) with the complete grouped specification -- opus rather than the usual sonnet-for-fully-specified-fixes pattern, given the cross-cutting scope (4 files, ~9 distinct behavioral changes that must compose correctly together) and the stakes (money-integrity + service-availability bugs), matching the user's fewer-stronger-passes guidance for exactly this kind of high-stakes dispatch.
Failed immediately on a 529 Overloaded (server-side capacity, explicitly flagged as usually-temporary) -- no output produced at all, essentially zero work lost. Resumed via SendMessage.
Failed AGAIN, same 529 Overloaded, still zero output -- second consecutive infra failure, not a code/task problem. User asked about progress vs. their quota (77% of 5hr window used at ~3h42m remaining, 86% of WEEKLY budget used, resets Thursday). Gave an honest assessment: this fix dispatch + its eventual re-review are each likely large (this session's biggest dispatches have run 150K-350K subagent tokens), full fix+re-review+PR probably doesn't comfortably fit remaining weekly budget, and weekly (not 5hr) is the binding constraint since it doesn't refresh again until Thursday. User asked for my recommendation; agreed plan: retry the fix (cheap, retries have been failing fast with near-zero cost), let it land, do MY OWN direct verification (not a subagent, low marginal cost), then STOP there deliberately rather than auto-continuing to the opus re-review -- leaves a clean, fully-implemented-and-independently-verified checkpoint regardless of whether more budget gets spent tonight or this resumes after refresh. Resumed the agent a third time -- FAILED AGAIN, identical 529 Overloaded, zero output all three times. Three consecutive identical failures is a different signal than one-off noise -- reads as a genuine, currently-persistent infra issue on the dispatch path itself (not this task, not this code), not something further immediate retries are likely to fix. Stopping the retry loop here rather than attempting a 4th blind retry; told the user directly rather than continuing to spend turns on retries with no new information. No code has been touched by any of the 3 failed attempts -- table.ts/handLog.ts/playerStore.ts/socketServer.ts are still exactly as the final review found them, HEAD is still 76d6dee, working tree still clean.
NEXT STEP ON RESUME: check whether the user wants another retry attempted now (infra may have cleared) or wants to pause until next session. If retrying: resume the same agent (a69a75ef8c61f8baa) again via SendMessage, same instructions as the last 3 attempts -- nothing about the fix specification has changed. Once it actually DONE/DONE_WITH_CONCERNS lands: verify the diff directly against the full 5-group spec myself (highest-stakes verification pass of the session, do not skip or abbreviate), checkpoint the ledger with that verification, THEN STOP -- per the explicit user-agreed plan, do NOT auto-dispatch the opus re-review this session regardless. Report back with a clean status and let the user decide on re-review + PR timing.
NEXT STEP ON RESUME (if interrupted): check for a completion notification before re-dispatching -- don't duplicate the agent (a69a75ef8c61f8baa); resume the same agent if it's an infrastructure failure, matching this session's established precedent. Once DONE/DONE_WITH_CONCERNS: verify the diff directly against the full spec above (all 5 groups) before doing anything else -- this is the highest-stakes verification pass of the session, do not skip or abbreviate it. Then generate a fresh review package (`scripts/review-package 76d6dee HEAD`, scoped to just this fix commit per the established fix-round precedent) and dispatch an OPUS-tier re-reviewer -- given the scale of this fix wave, consider whether the SAME final-review agent (a313b0f9bc03cef6c, still addressable) or a fresh one is better: the same agent already has full context on all 11 findings and doesn't need to rediscover them, which likely argues for resuming it via SendMessage same as every other fix-round precedent this session. If that re-review comes back clean (or only-Minor-left), proceed to superpowers:finishing-a-development-branch / present the PR option to the user (never push/open a PR without checking first, per standing risk rules) -- do NOT auto-merge or auto-push regardless of how clean the review comes back.
